# Copy to backend/.env and fill in. .env is gitignored.
# Free token from https://www.football-data.org/client/register
FOOTBALL_DATA_TOKEN=

# Optional overrides (TheSportsDB uses the free shared key by default)
# THESPORTSDB_KEY=123
# THESPORTSDB_WC_LEAGUE_ID=4429
